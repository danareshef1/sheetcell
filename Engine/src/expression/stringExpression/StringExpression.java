package expression.stringExpression;

import expression.Expression;
import expression.Number;
import expression.Text;
import expression.numericalExpression.NumericalExpression;
import expression.systemicExpression.SystemicExpression;

public interface StringExpression extends Expression {
//    public static boolean isStringExpression(Expression expression) {
//        // Check if the expression is an instance of a string expression
//        if (expression instanceof Text) {
//            return true; // It's a direct string value
//        }
//        if (expression instanceof SystemicExpression) {
//            return true;
//        }
//
//        return expression instanceof StringExpression; // It's a string function
//    }
}
